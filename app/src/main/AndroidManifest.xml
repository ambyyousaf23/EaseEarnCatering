<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qureshi.easeearncatering">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <permission
        android:name="com.example.qureshi.googlemapdemo.permission.MAPS_RECIEVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.qureshi.googlemapdemo.permission.MAPS_RECIEVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permissions.READ_GSERVICES" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mainicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

             <intent-filter>
             <action android:name="android.intent.action.MAIN" />


             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>
        <activity
            android:name=".navigation"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".create_account" />
        <activity android:name=".customize_card" />
        <activity android:name=".cardViewHolder" />
        <activity android:name=".cater_form" />
        <activity android:name=".customer_form" />
        <activity android:name=".addmenu" />
        <activity android:name=".submenu" />
        <activity android:name=".cater_login_view" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBxUo_AVkhR5G2XTXvEabjnFz4LPUldoC0" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".CaterRegistration"
            android:label="@string/title_activity_cater_registration"
            android:theme="@style/AppTheme" />
        <activity android:name=".ViewCaters" />
        <activity android:name=".ViewMenu" />
        <activity android:name=".order" />
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme" />
        <activity android:name=".ShowUsers" />
        <activity android:name=".CartActivity" />
        <activity android:name=".CaterView" />
        <activity android:name=".ViewOrders" />
        <activity android:name=".ReadInvoice" />
        <activity android:name=".Track_Order" />
        <activity
            android:name=".AddMainMenu"
            android:label="@string/title_activity_add_main_menu"
            android:theme="@style/AppTheme" />
        <activity android:name=".ViewMenuForCater" />
        <activity android:name=".AddSubMenu" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".Registration" />
        <activity android:name=".contactus"></activity>
    </application>

</manifest>